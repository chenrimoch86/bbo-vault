# Ingest Prompt

After running `make all` (or `make download && make convert`), paste this into
Claude Code inside your `knowledge_base/` folder to ingest the converted notes.

---

Added files to raw/, please ingest them.
